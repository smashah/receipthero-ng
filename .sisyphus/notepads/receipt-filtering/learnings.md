## Patterns and Conventions
- Paperless API uses query parameters like `tags__id__all` for matching all tags and `tags__id__none` for excluding tags.
- Environment variables are accessed via `process.env` and provided defaults where appropriate.
