{
  "name": "@sm-rn/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts & bun run --watch src/worker.ts",
    "start": "bun run src/index.ts & bun run src/worker.ts",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sm-rn/shared": "workspace:*",
    "hono": "catalog:",
    "@hono/zod-validator": "^0.4.2",
    "drizzle-orm": "^0.38.4",
    "together-ai": "catalog:",
    "zod": "catalog:",
    "@upstash/ratelimit": "catalog:",
    "@upstash/redis": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "drizzle-kit": "^0.31.5",
    "typescript": "catalog:"
  }
}
